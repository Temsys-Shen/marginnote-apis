---
openapi: 3.1.0
info:
  title: MarginNote CLI / Local Bridge API
  summary: MarginNote 本地 CLI Bridge 的 OpenAPI 3.1 草稿契约。
  description: 本文件根据当前 MarginNote Local Bridge 路由目录和 CLI 契约说明生成。当前仓库未发现官方手工维护的标准 OpenAPI
    文件。生成 client 或做自动化调用前，应先读取运行时 GET /capabilities 并比对 apiFingerprint；若静态文档与运行时能力冲突，以运行时为准。本文已标注
    iOS/iPadOS 跨设备限定接口；Mac 同机 CLI/Bridge 不注册这些限定路由。
  termsOfService: 本地内部评估草稿；仓库内未发布公开服务条款。
  contact:
    name: MarginNote Local Bridge 维护者
    url: about:blank
    email: 未公开
  license:
    name: 专有内部草稿
    identifier: LicenseRef-Proprietary
    url: about:blank
  version: 0.39-draft
externalDocs:
  description: 配对后可通过 GET /guide 读取运行时操作规程；静态契约来源为仓库内 CLI V0 契约文档。
  url: about:blank
servers:
- url: "/bridge/v1"
  description: MarginNote Bridge 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
  variables:
    port:
      default: runtime-discovered
      description: 从本地发现或跨设备配对流程获得的 Bridge 端口。
      enum:
      - runtime-discovered
security:
- bearerAuth: []
tags:
- name: status
  description: 状态、运行时能力和操作规程读取。
  externalDocs:
    description: 工作流级约束请读取运行时 guide。
    url: about:blank
- name: library
  description: 资料库、文档、文件夹和学习集创建相关操作。
  externalDocs:
    description: 工作流级约束请读取运行时 guide。
    url: about:blank
- name: study-sets
  description: 学习集、脑图树、子脑图、快照和学习集内文档操作。
  externalDocs:
    description: 工作流级约束请读取运行时 guide。
    url: about:blank
- name: documents
  description: 文档检索、页面、目录、定位、上传和删除操作。
  externalDocs:
    description: 工作流级约束请读取运行时 guide。
    url: about:blank
- name: notes
  description: 卡片内容、移动、复制、标签、元数据和渲染操作。
  externalDocs:
    description: 工作流级约束请读取运行时 guide。
    url: about:blank
- name: decks
  description: 复习卡组及卡组卡片操作。
  externalDocs:
    description: 工作流级约束请读取运行时 guide。
    url: about:blank
- name: bundles
  description: 拆书 bundle 的准备、提交、聚焦、丢弃和文件读取。
  externalDocs:
    description: 工作流级约束请读取运行时 guide。
    url: about:blank
- name: ui
  description: 当前上下文和 UI 状态读写。
  externalDocs:
    description: 工作流级约束请读取运行时 guide。
    url: about:blank
- name: setup
  description: 安装、配对和跨设备接入。
  externalDocs:
    description: 工作流级约束请读取运行时 guide。
    url: about:blank
- name: mcp
  description: Streamable HTTP MCP 接入。
  externalDocs:
    description: 工作流级约束请读取运行时 guide。
    url: about:blank
paths:
  "/":
    summary: "/"
    description: 跨设备配对根入口。仅 iOS/iPadOS 侧注册，用于给尚未持有 token 的客户端展示配对说明；Mac 同机 CLI/Bridge
      不注册。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - iOS
    - iPadOS
    x-platform-note: 仅跨设备接入场景注册；Mac Catalyst 同机 CLI/Bridge 构建不注册该路由。运行时请以 GET /capabilities
      返回的路由清单为准。
    get:
      operationId: get
      tags:
      - status
      summary: 读取跨设备配对入口说明
      security: []
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &1
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *1
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 跨设备配对根入口。仅 iOS/iPadOS 侧注册，用于给尚未持有 token 的客户端展示配对说明；Mac 同机 CLI/Bridge
        不注册。 静态文档不是最终真值，精确可用性以运行时 GET /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - iOS
      - iPadOS
      x-platform-note: 仅跨设备接入场景注册；Mac Catalyst 同机 CLI/Bridge 构建不注册该路由。运行时请以 GET /capabilities
        返回的路由清单为准。
  "/bundles":
    summary: "/bundles"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_bundles
      tags:
      - bundles
      summary: GET /bundles
      parameters:
      - name: limit
        in: query
        required: false
        description: 返回条目数量上限。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: limit 示例
            value: 1
      - name: cursor
        in: query
        required: false
        description: 分页游标。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: cursor-example
        examples:
          sample:
            summary: cursor 示例
            value: cursor-example
      - name: query
        in: query
        required: false
        description: 搜索关键词。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: query-example
        examples:
          sample:
            summary: query 示例
            value: query-example
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &2
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *2
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/bundles/prepare":
    summary: "/bundles/prepare"
    description: 基于文档页码或片段准备 AI 拆书 bundle。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_bundles_prepare
      tags:
      - bundles
      summary: POST /bundles/prepare
      requestBody:
        description: "/bundles/prepare 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/BundlePrepareRequest"
            example: &3
              topicid: topic-example
              bookmd5: bookmd5-example
              pageRange:
                startPage: 1
                endPage: 10
              preset: book-breakdown
              anchorMode: page_text
              title: 章节拆解
            examples:
              sample:
                summary: 请求示例
                value: *3
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &4
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *4
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 基于文档页码或片段准备 AI 拆书 bundle。 静态文档不是最终真值，精确可用性以运行时 GET /capabilities
        的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/bundles/{bundleId}/discard":
    summary: "/bundles/{bundleId}/discard"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_bundles_by_bundleId_discard
      tags:
      - bundles
      summary: POST /bundles/{bundleId}/discard
      parameters:
      - name: bundleId
        in: path
        required: true
        description: bundleId 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bundleId-example
        examples:
          sample:
            summary: bundleId 示例
            value: bundleId-example
      requestBody:
        description: "/bundles/{bundleId}/discard 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &5
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *5
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &6
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *6
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/bundles/{bundleId}/files":
    summary: "/bundles/{bundleId}/files"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_bundles_by_bundleId_files
      tags:
      - bundles
      summary: GET /bundles/{bundleId}/files
      parameters:
      - name: bundleId
        in: path
        required: true
        description: bundleId 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bundleId-example
        examples:
          sample:
            summary: bundleId 示例
            value: bundleId-example
      - name: limit
        in: query
        required: false
        description: 返回条目数量上限。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: limit 示例
            value: 1
      - name: cursor
        in: query
        required: false
        description: 分页游标。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: cursor-example
        examples:
          sample:
            summary: cursor 示例
            value: cursor-example
      - name: query
        in: query
        required: false
        description: 搜索关键词。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: query-example
        examples:
          sample:
            summary: query 示例
            value: query-example
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &7
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *7
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/bundles/{bundleId}/focus":
    summary: "/bundles/{bundleId}/focus"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_bundles_by_bundleId_focus
      tags:
      - bundles
      summary: POST /bundles/{bundleId}/focus
      parameters:
      - name: bundleId
        in: path
        required: true
        description: bundleId 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bundleId-example
        examples:
          sample:
            summary: bundleId 示例
            value: bundleId-example
      requestBody:
        description: "/bundles/{bundleId}/focus 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &8
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *8
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &9
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *9
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/bundles/{bundleId}/submit":
    summary: "/bundles/{bundleId}/submit"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_bundles_by_bundleId_submit
      tags:
      - bundles
      summary: POST /bundles/{bundleId}/submit
      parameters:
      - name: bundleId
        in: path
        required: true
        description: bundleId 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bundleId-example
        examples:
          sample:
            summary: bundleId 示例
            value: bundleId-example
      requestBody:
        description: "/bundles/{bundleId}/submit 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &10
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *10
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &11
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *11
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/capabilities":
    summary: "/capabilities"
    description: 返回运行时路由目录和能力元数据。客户端在信任静态 OpenAPI 前，应先比对 apiFingerprint。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_capabilities
      tags:
      - status
      summary: 读取运行时能力目录
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/CapabilitiesResponse"
              example: &12
                bridgeApiVersion: '0.39'
                routeCount: 66
                routes:
                - method: GET
                  path: "/status"
                presets:
                - book-breakdown
                - source-structure
                - exam-general
                - dictionary
                anchorModes:
                - page_text
                - block
                apiFingerprint: abcdef123456
              examples:
                sample:
                  summary: 成功响应示例
                  value: *12
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 返回运行时路由目录和能力元数据。客户端在信任静态 OpenAPI 前，应先比对 apiFingerprint。 静态文档不是最终真值，精确可用性以运行时
        GET /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/context":
    summary: "/context"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_context
      tags:
      - ui
      summary: GET /context
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &13
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *13
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/decks":
    summary: "/decks"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_decks
      tags:
      - decks
      summary: GET /decks
      parameters:
      - name: limit
        in: query
        required: false
        description: 返回条目数量上限。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: limit 示例
            value: 1
      - name: cursor
        in: query
        required: false
        description: 分页游标。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: cursor-example
        examples:
          sample:
            summary: cursor 示例
            value: cursor-example
      - name: query
        in: query
        required: false
        description: 搜索关键词。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: query-example
        examples:
          sample:
            summary: query 示例
            value: query-example
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &14
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *14
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_decks
      tags:
      - decks
      summary: POST /decks
      requestBody:
        description: "/decks 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &15
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *15
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &16
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *16
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/decks/{deckid}/cards":
    summary: "/decks/{deckid}/cards"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_decks_by_deckid_cards
      tags:
      - decks
      summary: GET /decks/{deckid}/cards
      parameters:
      - name: deckid
        in: path
        required: true
        description: deckid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: deckid-example
        examples:
          sample:
            summary: deckid 示例
            value: deckid-example
      - name: limit
        in: query
        required: false
        description: 返回条目数量上限。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: limit 示例
            value: 1
      - name: cursor
        in: query
        required: false
        description: 分页游标。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: cursor-example
        examples:
          sample:
            summary: cursor 示例
            value: cursor-example
      - name: query
        in: query
        required: false
        description: 搜索关键词。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: query-example
        examples:
          sample:
            summary: query 示例
            value: query-example
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &17
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *17
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_decks_by_deckid_cards
      tags:
      - decks
      summary: POST /decks/{deckid}/cards
      parameters:
      - name: deckid
        in: path
        required: true
        description: deckid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: deckid-example
        examples:
          sample:
            summary: deckid 示例
            value: deckid-example
      requestBody:
        description: "/decks/{deckid}/cards 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &18
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *18
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &19
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *19
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/decks/{deckid}/open":
    summary: "/decks/{deckid}/open"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_decks_by_deckid_open
      tags:
      - decks
      summary: POST /decks/{deckid}/open
      parameters:
      - name: deckid
        in: path
        required: true
        description: deckid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: deckid-example
        examples:
          sample:
            summary: deckid 示例
            value: deckid-example
      requestBody:
        description: "/decks/{deckid}/open 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &20
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *20
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &21
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *21
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/devices":
    summary: "/devices"
    description: 跨设备已配对设备列表。仅 iOS/iPadOS 侧注册，返回设备标识、名称和配对时间，不返回任何令牌材料。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - iOS
    - iPadOS
    x-platform-note: 仅跨设备接入场景注册；Mac Catalyst 同机 CLI/Bridge 构建不注册该路由。运行时请以 GET /capabilities
      返回的路由清单为准。
    get:
      operationId: get_devices
      tags:
      - status
      summary: 列出已配对设备
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &22
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *22
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 跨设备已配对设备列表。仅 iOS/iPadOS 侧注册，返回设备标识、名称和配对时间，不返回任何令牌材料。 静态文档不是最终真值，精确可用性以运行时
        GET /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - iOS
      - iPadOS
      x-platform-note: 仅跨设备接入场景注册；Mac Catalyst 同机 CLI/Bridge 构建不注册该路由。运行时请以 GET /capabilities
        返回的路由清单为准。
  "/documents/{bookmd5}/locate":
    summary: "/documents/{bookmd5}/locate"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_documents_by_bookmd5_locate
      tags:
      - documents
      summary: POST /documents/{bookmd5}/locate
      parameters:
      - name: bookmd5
        in: path
        required: true
        description: bookmd5 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bookmd5-example
        examples:
          sample:
            summary: bookmd5 示例
            value: bookmd5-example
      requestBody:
        description: "/documents/{bookmd5}/locate 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &23
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *23
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &24
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *24
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/documents/{bookmd5}/outline":
    summary: "/documents/{bookmd5}/outline"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_documents_by_bookmd5_outline
      tags:
      - documents
      summary: GET /documents/{bookmd5}/outline
      parameters:
      - name: bookmd5
        in: path
        required: true
        description: bookmd5 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bookmd5-example
        examples:
          sample:
            summary: bookmd5 示例
            value: bookmd5-example
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &25
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *25
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/documents/{bookmd5}/pages":
    summary: "/documents/{bookmd5}/pages"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_documents_by_bookmd5_pages
      tags:
      - documents
      summary: GET /documents/{bookmd5}/pages
      parameters:
      - name: bookmd5
        in: path
        required: true
        description: bookmd5 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bookmd5-example
        examples:
          sample:
            summary: bookmd5 示例
            value: bookmd5-example
      - name: start
        in: query
        required: false
        description: start 查询参数。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: start 示例
            value: 1
      - name: end
        in: query
        required: false
        description: end 查询参数。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: end 示例
            value: 1
      - name: blocks
        in: query
        required: false
        description: blocks 查询参数。
        schema:
          type: boolean
        deprecated: false
        allowEmptyValue: false
        example: blocks-example
        examples:
          sample:
            summary: blocks 示例
            value: blocks-example
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &26
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *26
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/documents/{bookmd5}/pages/{index}/content/{contentId}":
    summary: "/documents/{bookmd5}/pages/{index}/content/{contentId}"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_documents_by_bookmd5_pages_by_index_content_by_contentId
      tags:
      - documents
      summary: GET /documents/{bookmd5}/pages/{index}/content/{contentId}
      parameters:
      - name: bookmd5
        in: path
        required: true
        description: bookmd5 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bookmd5-example
        examples:
          sample:
            summary: bookmd5 示例
            value: bookmd5-example
      - name: index
        in: path
        required: true
        description: index 路径参数。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: index 示例
            value: 1
      - name: contentId
        in: path
        required: true
        description: contentId 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: contentId-example
        examples:
          sample:
            summary: contentId 示例
            value: contentId-example
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &27
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *27
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/documents/{bookmd5}/pages/{index}/parts":
    summary: "/documents/{bookmd5}/pages/{index}/parts"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_documents_by_bookmd5_pages_by_index_parts
      tags:
      - documents
      summary: GET /documents/{bookmd5}/pages/{index}/parts
      parameters:
      - name: bookmd5
        in: path
        required: true
        description: bookmd5 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bookmd5-example
        examples:
          sample:
            summary: bookmd5 示例
            value: bookmd5-example
      - name: index
        in: path
        required: true
        description: index 路径参数。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: index 示例
            value: 1
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &28
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *28
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/documents/{bookmd5}/toc":
    summary: "/documents/{bookmd5}/toc"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_documents_by_bookmd5_toc
      tags:
      - documents
      summary: POST /documents/{bookmd5}/toc
      parameters:
      - name: bookmd5
        in: path
        required: true
        description: bookmd5 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bookmd5-example
        examples:
          sample:
            summary: bookmd5 示例
            value: bookmd5-example
      requestBody:
        description: "/documents/{bookmd5}/toc 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ConfirmRequest"
            example: &29
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *29
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &30
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *30
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/documents/{bookmd5}/toc-candidates":
    summary: "/documents/{bookmd5}/toc-candidates"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_documents_by_bookmd5_toc_candidates
      tags:
      - documents
      summary: GET /documents/{bookmd5}/toc-candidates
      parameters:
      - name: bookmd5
        in: path
        required: true
        description: bookmd5 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bookmd5-example
        examples:
          sample:
            summary: bookmd5 示例
            value: bookmd5-example
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &31
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *31
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/guide":
    summary: "/guide"
    description: 返回当前 Bridge 内嵌的 Agent 操作规程 Markdown。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_guide
      tags:
      - status
      summary: 读取运行时操作规程
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GuideResponse"
              example: &32
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *32
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 返回当前 Bridge 内嵌的 Agent 操作规程 Markdown。 静态文档不是最终真值，精确可用性以运行时 GET /capabilities
        的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/install":
    summary: "/install"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_install
      tags:
      - setup
      summary: GET /install
      security: []
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &33
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *33
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/install/code":
    summary: "/install/code"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_install_code
      tags:
      - setup
      summary: POST /install/code
      requestBody:
        description: "/install/code 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &34
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *34
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &35
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *35
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/library/documents/upload":
    summary: "/library/documents/upload"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_library_documents_upload
      tags:
      - library
      summary: POST /library/documents/upload
      requestBody:
        description: "/library/documents/upload 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          multipart/form-data:
            schema:
              "$ref": "#/components/schemas/DocumentUploadRequest"
            example: &36
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *36
            encoding:
              file:
                contentType: application/pdf
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &37
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *37
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/library/documents/{bookmd5}/delete":
    summary: "/library/documents/{bookmd5}/delete"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_library_documents_by_bookmd5_delete
      tags:
      - library
      summary: POST /library/documents/{bookmd5}/delete
      parameters:
      - name: bookmd5
        in: path
        required: true
        description: bookmd5 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bookmd5-example
        examples:
          sample:
            summary: bookmd5 示例
            value: bookmd5-example
      requestBody:
        description: "/library/documents/{bookmd5}/delete 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ConfirmRequest"
            example: &38
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *38
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &39
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *39
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/library/folders":
    summary: "/library/folders"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_library_folders
      tags:
      - library
      summary: GET /library/folders
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &40
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *40
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters:
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/library/study-sets":
    summary: "/library/study-sets"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_library_study_sets
      tags:
      - library
      summary: GET /library/study-sets
      parameters:
      - name: limit
        in: query
        required: false
        description: 返回条目数量上限。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: limit 示例
            value: 1
      - name: cursor
        in: query
        required: false
        description: 分页游标。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: cursor-example
        examples:
          sample:
            summary: cursor 示例
            value: cursor-example
      - name: query
        in: query
        required: false
        description: 搜索关键词。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: query-example
        examples:
          sample:
            summary: query 示例
            value: query-example
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &41
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *41
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_library_study_sets
      tags:
      - library
      summary: POST /library/study-sets
      requestBody:
        description: "/library/study-sets 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &42
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *42
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &43
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *43
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/mcp":
    summary: "/mcp"
    description: Streamable HTTP MCP 端点。Bridge 暴露 mn_guide 和 mn_call，而不是把每个 REST 路由翻译成单独
      MCP tool。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_mcp
      tags:
      - mcp
      summary: POST /mcp
      requestBody:
        description: "/mcp 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/JsonRpcRequest"
            example: &44
              jsonrpc: '2.0'
              id: 1
              method: tools/list
              params: {}
            examples:
              sample:
                summary: 请求示例
                value: *44
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/JsonRpcResponse"
              example: &45
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *45
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: Streamable HTTP MCP 端点。Bridge 暴露 mn_guide 和 mn_call，而不是把每个 REST
        路由翻译成单独 MCP tool。 静态文档不是最终真值，精确可用性以运行时 GET /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/batch-get":
    summary: "/notes/batch-get"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_batch_get
      tags:
      - notes
      summary: POST /notes/batch-get
      requestBody:
        description: "/notes/batch-get 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &46
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *46
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &47
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *47
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/copy":
    summary: "/notes/copy"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_copy
      tags:
      - notes
      summary: POST /notes/copy
      requestBody:
        description: "/notes/copy 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/NoteCopyRequest"
            example: &48
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *48
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &49
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *49
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/delete":
    summary: "/notes/delete"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_delete
      tags:
      - notes
      summary: POST /notes/delete
      requestBody:
        description: "/notes/delete 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/NoteDeleteRequest"
            example: &50
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *50
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &51
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *51
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/fold":
    summary: "/notes/fold"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_fold
      tags:
      - notes
      summary: POST /notes/fold
      requestBody:
        description: "/notes/fold 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &52
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *52
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &53
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *53
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/hashtags":
    summary: "/notes/hashtags"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_hashtags
      tags:
      - notes
      summary: POST /notes/hashtags
      requestBody:
        description: "/notes/hashtags 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/NoteHashtagsRequest"
            example: &54
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *54
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &55
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *55
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/metadata":
    summary: "/notes/metadata"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_metadata
      tags:
      - notes
      summary: POST /notes/metadata
      requestBody:
        description: "/notes/metadata 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &56
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *56
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &57
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *57
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/move":
    summary: "/notes/move"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_move
      tags:
      - notes
      summary: POST /notes/move
      requestBody:
        description: "/notes/move 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &58
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *58
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &59
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *59
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/{noteid}/comments":
    summary: "/notes/{noteid}/comments"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_by_noteid_comments
      tags:
      - notes
      summary: POST /notes/{noteid}/comments
      parameters:
      - name: noteid
        in: path
        required: true
        description: noteid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: noteid-example
        examples:
          sample:
            summary: noteid 示例
            value: noteid-example
      requestBody:
        description: "/notes/{noteid}/comments 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &60
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *60
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &61
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *61
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/{noteid}/comments/{index}":
    summary: "/notes/{noteid}/comments/{index}"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_by_noteid_comments_by_index
      tags:
      - notes
      summary: POST /notes/{noteid}/comments/{index}
      parameters:
      - name: noteid
        in: path
        required: true
        description: noteid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: noteid-example
        examples:
          sample:
            summary: noteid 示例
            value: noteid-example
      - name: index
        in: path
        required: true
        description: index 路径参数。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: index 示例
            value: 1
      requestBody:
        description: "/notes/{noteid}/comments/{index} 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &62
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *62
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &63
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *63
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/{noteid}/comments/{index}/delete":
    summary: "/notes/{noteid}/comments/{index}/delete"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_by_noteid_comments_by_index_delete
      tags:
      - notes
      summary: POST /notes/{noteid}/comments/{index}/delete
      parameters:
      - name: noteid
        in: path
        required: true
        description: noteid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: noteid-example
        examples:
          sample:
            summary: noteid 示例
            value: noteid-example
      - name: index
        in: path
        required: true
        description: index 路径参数。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: index 示例
            value: 1
      requestBody:
        description: "/notes/{noteid}/comments/{index}/delete 的请求体。端点特有字段尽量从已冻结 CLI
          契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ConfirmRequest"
            example: &64
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *64
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &65
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *65
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/{noteid}/content/{contentId}":
    summary: "/notes/{noteid}/content/{contentId}"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_notes_by_noteid_content_by_contentId
      tags:
      - notes
      summary: GET /notes/{noteid}/content/{contentId}
      parameters:
      - name: noteid
        in: path
        required: true
        description: noteid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: noteid-example
        examples:
          sample:
            summary: noteid 示例
            value: noteid-example
      - name: contentId
        in: path
        required: true
        description: contentId 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: contentId-example
        examples:
          sample:
            summary: contentId 示例
            value: contentId-example
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &66
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *66
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/{noteid}/focus":
    summary: "/notes/{noteid}/focus"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_by_noteid_focus
      tags:
      - notes
      summary: POST /notes/{noteid}/focus
      parameters:
      - name: noteid
        in: path
        required: true
        description: noteid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: noteid-example
        examples:
          sample:
            summary: noteid 示例
            value: noteid-example
      requestBody:
        description: "/notes/{noteid}/focus 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &67
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *67
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &68
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *68
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/{noteid}/fold-into-sub-map":
    summary: "/notes/{noteid}/fold-into-sub-map"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_by_noteid_fold_into_sub_map
      tags:
      - notes
      summary: POST /notes/{noteid}/fold-into-sub-map
      parameters:
      - name: noteid
        in: path
        required: true
        description: noteid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: noteid-example
        examples:
          sample:
            summary: noteid 示例
            value: noteid-example
      requestBody:
        description: "/notes/{noteid}/fold-into-sub-map 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &69
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *69
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &70
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *70
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/{noteid}/merge-branch":
    summary: "/notes/{noteid}/merge-branch"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_by_noteid_merge_branch
      tags:
      - notes
      summary: POST /notes/{noteid}/merge-branch
      parameters:
      - name: noteid
        in: path
        required: true
        description: noteid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: noteid-example
        examples:
          sample:
            summary: noteid 示例
            value: noteid-example
      requestBody:
        description: "/notes/{noteid}/merge-branch 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &71
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *71
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &72
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *72
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/{noteid}/render":
    summary: "/notes/{noteid}/render"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_notes_by_noteid_render
      tags:
      - notes
      summary: GET /notes/{noteid}/render
      parameters:
      - name: noteid
        in: path
        required: true
        description: noteid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: noteid-example
        examples:
          sample:
            summary: noteid 示例
            value: noteid-example
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &73
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *73
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/{noteid}/text":
    summary: "/notes/{noteid}/text"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_by_noteid_text
      tags:
      - notes
      summary: POST /notes/{noteid}/text
      parameters:
      - name: noteid
        in: path
        required: true
        description: noteid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: noteid-example
        examples:
          sample:
            summary: noteid 示例
            value: noteid-example
      requestBody:
        description: "/notes/{noteid}/text 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &74
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *74
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &75
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *75
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/notes/{noteid}/title":
    summary: "/notes/{noteid}/title"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_notes_by_noteid_title
      tags:
      - notes
      summary: POST /notes/{noteid}/title
      parameters:
      - name: noteid
        in: path
        required: true
        description: noteid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: noteid-example
        examples:
          sample:
            summary: noteid 示例
            value: noteid-example
      requestBody:
        description: "/notes/{noteid}/title 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &76
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *76
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &77
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *77
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/pair":
    summary: "/pair"
    description: 跨设备配对接口。仅 iOS/iPadOS 侧注册，用 6 位配对码换取后续请求使用的 Bearer token。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - iOS
    - iPadOS
    x-platform-note: 仅跨设备接入场景注册；Mac Catalyst 同机 CLI/Bridge 构建不注册该路由。运行时请以 GET /capabilities
      返回的路由清单为准。
    post:
      operationId: post_pair
      tags:
      - setup
      summary: 用配对码换取访问 token
      security: []
      requestBody:
        description: "/pair 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &78
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *78
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &79
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *79
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 跨设备配对接口。仅 iOS/iPadOS 侧注册，用 6 位配对码换取后续请求使用的 Bearer token。 静态文档不是最终真值，精确可用性以运行时
        GET /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - iOS
      - iPadOS
      x-platform-note: 仅跨设备接入场景注册；Mac Catalyst 同机 CLI/Bridge 构建不注册该路由。运行时请以 GET /capabilities
        返回的路由清单为准。
  "/search":
    summary: "/search"
    description: 搜索学习集、卡片和文档。不要假设存在完整文档列表接口，找书优先使用该接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_search
      tags:
      - status
      summary: GET /search
      parameters:
      - name: q
        in: query
        required: false
        description: q 查询参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: q-example
        examples:
          sample:
            summary: q 示例
            value: q-example
      - name: scope
        in: query
        required: false
        description: scope 查询参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: scope-example
        examples:
          sample:
            summary: scope 示例
            value: scope-example
      - name: limit
        in: query
        required: false
        description: 返回条目数量上限。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: limit 示例
            value: 1
      - name: cursor
        in: query
        required: false
        description: 分页游标。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: cursor-example
        examples:
          sample:
            summary: cursor 示例
            value: cursor-example
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &80
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *80
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 搜索学习集、卡片和文档。不要假设存在完整文档列表接口，找书优先使用该接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/snapshots/{snapshotId}/restore":
    summary: "/snapshots/{snapshotId}/restore"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_snapshots_by_snapshotId_restore
      tags:
      - study-sets
      summary: POST /snapshots/{snapshotId}/restore
      parameters:
      - name: snapshotId
        in: path
        required: true
        description: snapshotId 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: snapshotId-example
        examples:
          sample:
            summary: snapshotId 示例
            value: snapshotId-example
      requestBody:
        description: "/snapshots/{snapshotId}/restore 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ConfirmRequest"
            example: &81
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *81
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &82
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *82
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/status":
    summary: "/status"
    description: 读取 Bridge 状态、授权状态、路由数量和 API 指纹。该路由故意不要求鉴权，用于本地发现和陈旧文档检测。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_status
      tags:
      - status
      summary: 读取 Bridge 状态
      security: []
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/StatusResponse"
              example: &83
                bridgeApiVersion: '0.39'
                app: MarginNote 4
                enabled: true
                authorized: true
                routeCount: 66
                apiFingerprint: abcdef123456
              examples:
                sample:
                  summary: 成功响应示例
                  value: *83
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 读取 Bridge 状态、授权状态、路由数量和 API 指纹。该路由故意不要求鉴权，用于本地发现和陈旧文档检测。 静态文档不是最终真值，精确可用性以运行时
        GET /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/study-sets/{topicid}/delete":
    summary: "/study-sets/{topicid}/delete"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_study_sets_by_topicid_delete
      tags:
      - study-sets
      summary: POST /study-sets/{topicid}/delete
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      requestBody:
        description: "/study-sets/{topicid}/delete 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ConfirmRequest"
            example: &84
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *84
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &85
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *85
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/study-sets/{topicid}/documents":
    summary: "/study-sets/{topicid}/documents"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_study_sets_by_topicid_documents
      tags:
      - study-sets
      summary: GET /study-sets/{topicid}/documents
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      - name: limit
        in: query
        required: false
        description: 返回条目数量上限。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: limit 示例
            value: 1
      - name: cursor
        in: query
        required: false
        description: 分页游标。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: cursor-example
        examples:
          sample:
            summary: cursor 示例
            value: cursor-example
      - name: query
        in: query
        required: false
        description: 搜索关键词。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: query-example
        examples:
          sample:
            summary: query 示例
            value: query-example
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &86
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *86
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_study_sets_by_topicid_documents
      tags:
      - study-sets
      summary: POST /study-sets/{topicid}/documents
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      requestBody:
        description: "/study-sets/{topicid}/documents 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &87
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *87
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &88
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *88
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/study-sets/{topicid}/documents/{bookmd5}/delete":
    summary: "/study-sets/{topicid}/documents/{bookmd5}/delete"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_study_sets_by_topicid_documents_by_bookmd5_delete
      tags:
      - study-sets
      summary: POST /study-sets/{topicid}/documents/{bookmd5}/delete
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      - name: bookmd5
        in: path
        required: true
        description: bookmd5 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: bookmd5-example
        examples:
          sample:
            summary: bookmd5 示例
            value: bookmd5-example
      requestBody:
        description: "/study-sets/{topicid}/documents/{bookmd5}/delete 的请求体。端点特有字段尽量从已冻结
          CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/ConfirmRequest"
            example: &89
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *89
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &90
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *90
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/study-sets/{topicid}/hashtags":
    summary: "/study-sets/{topicid}/hashtags"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_study_sets_by_topicid_hashtags
      tags:
      - study-sets
      summary: GET /study-sets/{topicid}/hashtags
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &91
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *91
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/study-sets/{topicid}/note-palette":
    summary: "/study-sets/{topicid}/note-palette"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_study_sets_by_topicid_note_palette
      tags:
      - study-sets
      summary: GET /study-sets/{topicid}/note-palette
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &92
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *92
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/study-sets/{topicid}/notebooks":
    summary: "/study-sets/{topicid}/notebooks"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_study_sets_by_topicid_notebooks
      tags:
      - study-sets
      summary: GET /study-sets/{topicid}/notebooks
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &93
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *93
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/study-sets/{topicid}/open":
    summary: "/study-sets/{topicid}/open"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_study_sets_by_topicid_open
      tags:
      - study-sets
      summary: POST /study-sets/{topicid}/open
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      requestBody:
        description: "/study-sets/{topicid}/open 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &94
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *94
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &95
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *95
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/study-sets/{topicid}/snapshots":
    summary: "/study-sets/{topicid}/snapshots"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_study_sets_by_topicid_snapshots
      tags:
      - study-sets
      summary: GET /study-sets/{topicid}/snapshots
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &96
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *96
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_study_sets_by_topicid_snapshots
      tags:
      - study-sets
      summary: POST /study-sets/{topicid}/snapshots
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      requestBody:
        description: "/study-sets/{topicid}/snapshots 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &97
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *97
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &98
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *98
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/study-sets/{topicid}/sub-maps":
    summary: "/study-sets/{topicid}/sub-maps"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_study_sets_by_topicid_sub_maps
      tags:
      - study-sets
      summary: GET /study-sets/{topicid}/sub-maps
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &99
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *99
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/study-sets/{topicid}/tree":
    summary: "/study-sets/{topicid}/tree"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_study_sets_by_topicid_tree
      tags:
      - study-sets
      summary: GET /study-sets/{topicid}/tree
      parameters:
      - name: topicid
        in: path
        required: true
        description: topicid 路径参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: topicid-example
        examples:
          sample:
            summary: topicid 示例
            value: topicid-example
      - name: root
        in: query
        required: false
        description: root 查询参数。
        schema:
          type: string
        deprecated: false
        allowEmptyValue: false
        example: root-example
        examples:
          sample:
            summary: root 示例
            value: root-example
      - name: maxDepth
        in: query
        required: false
        description: maxDepth 查询参数。
        schema:
          type: integer
        deprecated: false
        allowEmptyValue: false
        example: 1
        examples:
          sample:
            summary: maxDepth 示例
            value: 1
      - name: limit
        in: query
        description: 返回条目数量上限。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: integer
          minimum: 1
          maximum: 200
        example: 100
        examples:
          sample:
            summary: limit 示例
            value: 100
      - name: cursor
        in: query
        description: 分页游标。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: cursor-token
        examples:
          sample:
            summary: cursor 示例
            value: cursor-token
      - name: query
        in: query
        description: 搜索关键词。
        required: false
        deprecated: false
        allowEmptyValue: false
        schema:
          type: string
        example: keyword
        examples:
          sample:
            summary: query 示例
            value: keyword
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &100
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *100
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/ui-state":
    summary: "/ui-state"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    get:
      operationId: get_ui_state
      tags:
      - ui
      summary: GET /ui-state
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ListResponse"
              example: &101
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *101
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
  "/ui-state/apply":
    summary: "/ui-state/apply"
    description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。
    servers:
    - "$ref": "#/servers/0"
    parameters: []
    x-platforms:
    - macOS
    - iOS
    - iPadOS
    x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
    post:
      operationId: post_ui_state_apply
      tags:
      - ui
      summary: POST /ui-state/apply
      requestBody:
        description: "/ui-state/apply 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
        required: false
        content:
          application/json:
            schema:
              "$ref": "#/components/schemas/GenericJsonRequest"
            example: &102
              confirm: true
            examples:
              sample:
                summary: 请求示例
                value: *102
            encoding: {}
      responses:
        '200':
          description: 请求成功。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/GenericSuccessResponse"
              example: &103
                bridgeApiVersion: '0.39'
              examples:
                sample:
                  summary: 成功响应示例
                  value: *103
          links: {}
        '400':
          "$ref": "#/components/responses/BadRequest"
        '401':
          "$ref": "#/components/responses/Unauthorized"
        '403':
          "$ref": "#/components/responses/Forbidden"
        '404':
          "$ref": "#/components/responses/NotFound"
        '409':
          "$ref": "#/components/responses/Conflict"
        '422':
          "$ref": "#/components/responses/UnprocessableEntity"
        '500':
          "$ref": "#/components/responses/InternalError"
        default:
          description: 错误响应。Bridge 使用统一错误 envelope，包含机器可读 code、人类可读 message、retryable
            标记和可选 details 对象。
          headers:
            Content-Type:
              "$ref": "#/components/headers/JsonContentType"
          content:
            application/json:
              schema:
                "$ref": "#/components/schemas/ErrorEnvelope"
              example:
                bridgeApiVersion: '0.39'
                error:
                  code: INVALID_INPUT
                  message: 请求无效。
                  retryable: false
                  details: {}
              examples:
                invalidInput:
                  "$ref": "#/components/examples/InvalidInputError"
                unauthorized:
                  "$ref": "#/components/examples/UnauthorizedError"
          links: {}
      description: 根据当前 MarginNote Local Bridge 路由目录生成的接口。 静态文档不是最终真值，精确可用性以运行时 GET
        /capabilities 的路由清单为准。
      parameters: []
      callbacks: {}
      deprecated: false
      security:
      - bearerAuth: []
      servers:
      - url: "/bridge/v1"
        description: 本地 Bridge HTTP 服务。主机和端口由运行时发现，本共享草稿故意不写入网络地址。
        variables:
          port:
            default: runtime-discovered
            description: 从本地 Bridge 元数据或配对流程发现的运行时端口。
            enum:
            - runtime-discovered
      x-platforms:
      - macOS
      - iOS
      - iPadOS
      x-platform-note: 通用 Local Bridge 路由；具体可用性仍以运行时 GET /capabilities 为准。
components:
  schemas:
    GenericJsonRequest:
      type: object
      description: 通用 JSON 请求占位 schema。当前代码库未给所有端点暴露逐字段 schema 单一事实源时使用。
      additionalProperties: true
    ConfirmRequest:
      type: object
      description: 破坏性操作或覆盖操作的确认门。
      properties:
        confirm:
          type: boolean
          const: true
          description: 端点要求显式确认时必须为 true。
      additionalProperties: true
    BundlePrepareRequest:
      type: object
      description: 从文档片段或页码范围准备拆书 bundle。
      properties:
        topicid:
          type: string
        bookmd5:
          type: string
        segments:
          type: array
          items:
            "$ref": "#/components/schemas/DocumentSegment"
        pageRange:
          "$ref": "#/components/schemas/PageRange"
        preset:
          type: string
          enum:
          - book-breakdown
          - source-structure
          - exam-general
          - dictionary
        anchorMode:
          type: string
          enum:
          - page_text
          - block
        title:
          type: string
      additionalProperties: true
    DocumentSegment:
      type: object
      properties:
        title:
          type: string
        startPage:
          type: integer
          minimum: 1
        endPage:
          type: integer
          minimum: 1
      additionalProperties: true
    PageRange:
      type: object
      properties:
        startPage:
          type: integer
          minimum: 1
        endPage:
          type: integer
          minimum: 1
      additionalProperties: false
    DocumentUploadRequest:
      type: object
      description: multipart 文件上传载荷。
      properties:
        file:
          type: string
          format: binary
        folderId:
          type: string
      required:
      - file
    NoteCopyRequest:
      type: object
      properties:
        noteIds:
          type: array
          items:
            type: string
          maxItems: 50
        targetParentNoteId:
          type: string
        mode:
          type: string
          enum:
          - reference
          - clone
        copies:
          type: array
          items:
            type: object
            additionalProperties: true
        confirm:
          type: boolean
      additionalProperties: true
    NoteHashtagsRequest:
      type: object
      properties:
        noteIds:
          type: array
          items:
            type: string
          maxItems: 200
        operation:
          type: string
          enum:
          - add
          - remove
          - replace
        hashtags:
          type: array
          items:
            type: string
        includeDescendants:
          type: boolean
        confirm:
          type: boolean
      additionalProperties: true
    NoteDeleteRequest:
      type: object
      properties:
        noteIds:
          type: array
          items:
            type: string
          minItems: 1
          maxItems: 50
        confirm:
          type: boolean
          const: true
      additionalProperties: true
    JsonRpcRequest:
      type: object
      required:
      - jsonrpc
      - method
      properties:
        jsonrpc:
          type: string
          const: '2.0'
        id:
          oneOf:
          - type: string
          - type: integer
          - type: 'null'
        method:
          type: string
        params:
          type: object
          additionalProperties: true
    JsonRpcResponse:
      type: object
      properties:
        jsonrpc:
          type: string
          const: '2.0'
        id:
          oneOf:
          - type: string
          - type: integer
          - type: 'null'
        result:
          type: object
          additionalProperties: true
        error:
          type: object
          additionalProperties: true
      additionalProperties: true
    GenericSuccessResponse:
      type: object
      required:
      - bridgeApiVersion
      properties:
        bridgeApiVersion:
          type: string
          description: Bridge API 版本。
        ignoredFields:
          type: array
          items:
            type: string
        warnings:
          type: array
          items:
            type: object
            additionalProperties: true
      additionalProperties: true
    ListResponse:
      allOf:
      - "$ref": "#/components/schemas/GenericSuccessResponse"
      - type: object
        properties:
          items:
            type: array
            items:
              type: object
              additionalProperties: true
          nextCursor:
            oneOf:
            - type: string
            - type: 'null'
          truncated:
            type: boolean
        additionalProperties: true
    StatusResponse:
      allOf:
      - "$ref": "#/components/schemas/GenericSuccessResponse"
      - type: object
        properties:
          app:
            type: string
          appVersion:
            type: string
          instanceNonce:
            type: string
          enabled:
            type: boolean
          authorized:
            type: boolean
          routeCount:
            type: integer
          apiFingerprint:
            type: string
            pattern: "^[0-9a-f]{12}$"
          guideVersion:
            type: string
          guideHash:
            type: string
          guide:
            "$ref": "#/components/schemas/GuideInfo"
        additionalProperties: true
    GuideInfo:
      type: object
      properties:
        version:
          type: string
        sha256:
          type: string
        route:
          type: string
        refreshPolicy:
          type: string
        usesExistingBearer:
          type: boolean
        installRequired:
          type: boolean
        rotatesToken:
          type: boolean
      additionalProperties: true
    CapabilitiesResponse:
      allOf:
      - "$ref": "#/components/schemas/GenericSuccessResponse"
      - type: object
        properties:
          routes:
            type: array
            items:
              "$ref": "#/components/schemas/RouteDescriptor"
          routeCount:
            type: integer
          presets:
            type: array
            items:
              type: string
          anchorModes:
            type: array
            items:
              type: string
          apiFingerprint:
            type: string
          guide:
            "$ref": "#/components/schemas/GuideInfo"
          bundlePrepare:
            type: object
            additionalProperties: true
          noteHashtags:
            type: object
            additionalProperties: true
          noteCopy:
            type: object
            additionalProperties: true
        additionalProperties: true
    RouteDescriptor:
      type: object
      required:
      - method
      - path
      properties:
        method:
          type: string
          enum:
          - GET
          - POST
          - PUT
          - PATCH
          - DELETE
          - OPTIONS
          - HEAD
          - TRACE
        path:
          type: string
      additionalProperties: true
    GuideResponse:
      allOf:
      - "$ref": "#/components/schemas/GenericSuccessResponse"
      - type: object
        properties:
          markdown:
            type: string
          guide:
            "$ref": "#/components/schemas/GuideInfo"
        additionalProperties: true
    ErrorEnvelope:
      type: object
      required:
      - bridgeApiVersion
      - error
      properties:
        bridgeApiVersion:
          type: string
        error:
          "$ref": "#/components/schemas/ErrorObject"
      additionalProperties: false
    ErrorObject:
      type: object
      required:
      - code
      - message
      - retryable
      properties:
        code:
          type: string
        message:
          type: string
        retryable:
          type: boolean
        details:
          type: object
          additionalProperties: true
      additionalProperties: true
  responses:
    BadRequest:
      description: 请求格式错误或语法无效。
      headers:
        Content-Type:
          "$ref": "#/components/headers/JsonContentType"
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/ErrorEnvelope"
          example:
            bridgeApiVersion: '0.39'
            error:
              code: INVALID_INPUT
              message: 请求无效。
              retryable: false
              details: {}
          examples:
            invalidInput:
              "$ref": "#/components/examples/InvalidInputError"
            unauthorized:
              "$ref": "#/components/examples/UnauthorizedError"
      links: {}
    Unauthorized:
      description: 缺少 Bearer token 或 token 无效。
      headers:
        Content-Type:
          "$ref": "#/components/headers/JsonContentType"
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/ErrorEnvelope"
          example:
            bridgeApiVersion: '0.39'
            error:
              code: INVALID_INPUT
              message: 请求无效。
              retryable: false
              details: {}
          examples:
            invalidInput:
              "$ref": "#/components/examples/InvalidInputError"
            unauthorized:
              "$ref": "#/components/examples/UnauthorizedError"
      links: {}
    Forbidden:
      description: 请求被本地安全策略拒绝，例如 Host 或 Origin 检查未通过。
      headers:
        Content-Type:
          "$ref": "#/components/headers/JsonContentType"
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/ErrorEnvelope"
          example:
            bridgeApiVersion: '0.39'
            error:
              code: INVALID_INPUT
              message: 请求无效。
              retryable: false
              details: {}
          examples:
            invalidInput:
              "$ref": "#/components/examples/InvalidInputError"
            unauthorized:
              "$ref": "#/components/examples/UnauthorizedError"
      links: {}
    NotFound:
      description: 资源或路由不存在。
      headers:
        Content-Type:
          "$ref": "#/components/headers/JsonContentType"
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/ErrorEnvelope"
          example:
            bridgeApiVersion: '0.39'
            error:
              code: INVALID_INPUT
              message: 请求无效。
              retryable: false
              details: {}
          examples:
            invalidInput:
              "$ref": "#/components/examples/InvalidInputError"
            unauthorized:
              "$ref": "#/components/examples/UnauthorizedError"
      links: {}
    Conflict:
      description: 状态冲突，例如内容已变化或资源正在使用。
      headers:
        Content-Type:
          "$ref": "#/components/headers/JsonContentType"
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/ErrorEnvelope"
          example:
            bridgeApiVersion: '0.39'
            error:
              code: INVALID_INPUT
              message: 请求无效。
              retryable: false
              details: {}
          examples:
            invalidInput:
              "$ref": "#/components/examples/InvalidInputError"
            unauthorized:
              "$ref": "#/components/examples/UnauthorizedError"
      links: {}
    UnprocessableEntity:
      description: 请求语义无效，例如缺少确认、范围无效或枚举值不支持。
      headers:
        Content-Type:
          "$ref": "#/components/headers/JsonContentType"
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/ErrorEnvelope"
          example:
            bridgeApiVersion: '0.39'
            error:
              code: INVALID_INPUT
              message: 请求无效。
              retryable: false
              details: {}
          examples:
            invalidInput:
              "$ref": "#/components/examples/InvalidInputError"
            unauthorized:
              "$ref": "#/components/examples/UnauthorizedError"
      links: {}
    InternalError:
      description: Bridge 内部异常。
      headers:
        Content-Type:
          "$ref": "#/components/headers/JsonContentType"
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/ErrorEnvelope"
          example:
            bridgeApiVersion: '0.39'
            error:
              code: INVALID_INPUT
              message: 请求无效。
              retryable: false
              details: {}
          examples:
            invalidInput:
              "$ref": "#/components/examples/InvalidInputError"
            unauthorized:
              "$ref": "#/components/examples/UnauthorizedError"
      links: {}
  parameters:
    Limit:
      name: limit
      in: query
      description: 返回条目数量上限。
      required: false
      deprecated: false
      allowEmptyValue: false
      schema:
        type: integer
        minimum: 1
        maximum: 200
      example: 100
    Cursor:
      name: cursor
      in: query
      description: 分页游标。
      required: false
      deprecated: false
      allowEmptyValue: false
      schema:
        type: string
      example: cursor-token
    Query:
      name: query
      in: query
      description: 搜索关键词。
      required: false
      deprecated: false
      allowEmptyValue: false
      schema:
        type: string
      example: keyword
  examples:
    InvalidInputError:
      summary: 输入无效
      value:
        bridgeApiVersion: '0.39'
        error:
          code: INVALID_INPUT
          message: 输入无效。
          retryable: false
          details: {}
    UnauthorizedError:
      summary: 未授权
      value:
        bridgeApiVersion: '0.39'
        error:
          code: UNAUTHORIZED
          message: 缺少 token 或 token 无效。
          retryable: false
          details: {}
    ConfirmRequiredError:
      summary: 需要确认
      value:
        bridgeApiVersion: '0.39'
        error:
          code: CONFIRM_REQUIRED
          message: 需要 confirm:true。
          retryable: false
          details: {}
  requestBodies:
    GenericJsonRequest:
      description: "/generic 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
      required: false
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/GenericJsonRequest"
          example: &104
            confirm: true
          examples:
            sample:
              summary: 请求示例
              value: *104
          encoding: {}
    BundlePrepareRequest:
      description: "/bundles/prepare 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
      required: false
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/BundlePrepareRequest"
          example: &105
            topicid: topic-example
            bookmd5: bookmd5-example
            pageRange:
              startPage: 1
              endPage: 10
            preset: book-breakdown
            anchorMode: page_text
            title: 章节拆解
          examples:
            sample:
              summary: 请求示例
              value: *105
          encoding: {}
    JsonRpcRequest:
      description: "/mcp 的请求体。端点特有字段尽量从已冻结 CLI 契约推断；除非端点另有严格校验说明，运行时可能忽略未知请求字段。"
      required: false
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/JsonRpcRequest"
          example: &106
            jsonrpc: '2.0'
            id: 1
            method: tools/list
            params: {}
          examples:
            sample:
              summary: 请求示例
              value: *106
          encoding: {}
    ConfirmRequest:
      description: 通用确认请求体。
      required: true
      content:
        application/json:
          schema:
            "$ref": "#/components/schemas/ConfirmRequest"
          example:
            confirm: true
          examples:
            confirm:
              summary: 确认示例
              value:
                confirm: true
          encoding: {}
  headers:
    JsonContentType:
      description: JSON 响应内容类型。
      schema:
        type: string
        example: application/json; charset=utf-8
  securitySchemes:
    bearerAuth:
      type: http
      description: 本地配对 token。不要写入日志、截图或生成客户端的调试输出。
      scheme: bearer
      bearerFormat: MN bridge local token
    apiKeyHeaderAuth:
      type: apiKey
      description: 仅用于覆盖 OpenAPI Security Scheme 结构；当前 Bridge 契约实际使用 bearerAuth。
      name: Authorization
      in: header
    oauth2Placeholder:
      type: oauth2
      description: OAuth2 flows 结构占位；当前本地 Bridge 不使用 OAuth2。
      flows:
        authorizationCode:
          authorizationUrl: about:blank
          tokenUrl: about:blank
          scopes: {}
    openIdConnectPlaceholder:
      type: openIdConnect
      description: openIdConnectUrl 字段结构占位；当前本地 Bridge 不使用 OpenID Connect。
      openIdConnectUrl: about:blank
  links:
    RefreshCapabilities:
      operationId: get_capabilities
      description: 检测到 routeCount、guideHash 或 apiFingerprint 变化后刷新运行时能力。
  callbacks: {}
webhooks: {}
